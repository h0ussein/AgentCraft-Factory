{
  "name": "my_ai-project",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && python run_server.py",
    "serve": "npm run build && npm run start"
  }
}
